# GitHub 자동 업로드 시도 기록 (2026-08-06 기준)

## 목표
`index.html`(lg_kpi_manager.html)을 수정할 때마다 사람이 직접 GitHub 업로드 페이지에
드래그앤드롭하지 않고, Claude가 자동으로 `karuguns2-bit/gyeongbuk2` 저장소에 올려서
Vercel(gyeongbuk2.vercel.app)까지 자동 배포되게 하고 싶음.

## 지금까지 막힌 이유 (전부 확인 완료, 현재는 방법 없음)

1. **브라우저 자동화(Claude in Chrome) 경로**
   - `mcp__claude-in-chrome__file_upload` 도구로 GitHub 업로드 페이지의 파일 입력창에
     파일을 직접 넣으려는 시도 → 매번 다음 오류로 거부됨:
     `Cannot upload "...": only files this session is allowed to read can be uploaded.`
   - Cowork에서 연결한 폴더(바탕화면, AI 폴더 등)의 파일 경로로 시도 → 거부됨
   - 채팅에 직접 첨부(드래그)한 파일의 실제 경로로 시도 → 이것도 거부됨
     (도구 설명에는 "attachments"는 허용된다고 나와있는데 실제로는 막힘 — Cowork 쪽 버그로 추정)
   - Chrome 확장 프로그램(`chrome-extension://fcoeoabgfenejglbffodgkkbkcdhcgfn`) 설정 페이지도
     사용자가 직접 전부 확인함 — 권한/단축키/옵션 3개 탭 어디에도 "파일 URL 액세스" 같은
     관련 설정이 없음
   - `chrome://extensions`의 "파일 URL 액세스 허용" 토글도 사용자가 켜봤지만 효과 없음

2. **터미널(git/GitHub API) 직접 push 경로**
   - Claude의 리눅스 샌드박스에서 `git push` 또는 GitHub REST API를 직접 호출해서
     브라우저를 거치지 않고 올리는 방법도 검토함
   - 샌드박스의 아웃바운드 네트워크가 화이트리스트 방식이라 `github.com`,
     `api.github.com`, `vercel.com` 전부 `403 blocked-by-allowlist`로 막혀 있어서
     애초에 접속 자체가 불가능함 (자격증명 문제가 아니라 네트워크 접근 자체가 차단됨)
   - (참고: 설령 네트워크가 열려도, 사용자의 GitHub 비밀번호/토큰을 Claude가 직접
     입력해서 인증하는 것은 정책상 금지되어 있어 이 경로는 애초에 다른 방식—예를 들어
     사용자가 직접 브라우저에서 OAuth 인증하는 방식—이 필요함)

## 결론
현재 Cowork 환경에서는 사람이 직접 GitHub 업로드 페이지에 드래그앤드롭 + Commit 버튼을
누르는 것 외에 자동화할 방법이 없음. 이건 사용자 설정으로 풀 수 있는 문제가 아니라
Cowork/Claude in Chrome 쪽 세션 권한 구조의 제약으로 보임.

## 2026-08-06 추가 시도 (탭그룹 재연결)
사용자 제안으로 브라우저 탭 그룹을 완전히 닫고 새로 생성한 뒤 재시도함.
- 탭 그룹을 새로 만들어도 동일하게 거부됨 → **탭 그룹 문제라는 가설은 틀렸음** (정정)
- 시도 중 새로운 오류 발견: 같은 내용의 파일을 여러 폴더에 복사해두면 시스템이 하드링크로
  중복 제거를 하는데, `file_upload`는 하드링크(Links>1) 걸린 파일을 별도로 거부함
  (오류: "the file has multiple hard links... Copy the file and upload the copy")
- 하드링크 없는 새 사본을 만들어 재시도 → 하드링크 오류는 사라졌지만 결국 다시
  "세션에 공유되지 않은 파일" 오류로 돌아옴
- 결론: 탭 그룹이나 하드링크는 곁가지 이슈였고, 근본 원인은 이 세션 자체가
  로컬 파일 경로를 브라우저에 업로드할 권한을 아예 부여받지 못한 상태라는 것.
  탭 그룹을 새로 만드는 것으로는 해결되지 않음.

## 다음에 다시 시도해볼 만한 것 (아직 안 해본 것)
- Cowork 앱 자체가 업데이트되면서 `file_upload`의 "attachments/outputs 폴더 허용" 설명이
  실제로 동작하게 바뀌었는지 재확인
- 샌드박스 네트워크 화이트리스트에 github.com이 추가되는 업데이트가 있었는지 확인
- Anthropic에 피드백(대화창 엄지 아이콘)을 이미 넣었다면 그 이후 패치 노트 확인

## 만약 이 메모를 다시 보게 된다면
새 대화에서 "자동 업로드 다시 시도해줘"라고 하면, 이 파일을 참고해서 이미 막혔던
방법들은 건너뛰고 위 "다음에 시도해볼 것" 항목부터 확인하면 됨.

## 2026-08-08 추가 시도 (재확인, 여전히 막힘)
경쟁력 UI 후속수정 배포 건으로 다시 시도함.
- `file_upload`에 전체 경로(`C:\Users\10950\OneDrive\Desktop\AI\lg_kpi_deploy\app.js`)를 넣으면
  파라미터 자체가 파싱 단계에서 깨짐(`Invalid input: expected array, received undefined`).
  경로를 한 단계씩 줄여가며 이분탐색한 결과, 짧은 경로(`test.txt`, `AI\lg_kpi_deploy\app.js`,
  `10950\OneDrive\Desktop\AI\lg_kpi_deploy\app.js` 등)는 전부 정상적으로 파싱되어
  "세션에 공유되지 않은 파일" 권한 오류까지 도달하는데, 유독 **드라이브 문자(`C:\Users\`)로
  시작하는 전체 경로 + 파일명이 `app.js`인 조합**만 파라미터 파싱 자체가 실패함
  (같은 전체경로에 `test.txt`로 이름만 바꾸면 파싱은 통과하고 권한 오류로 감).
  → 파일명 자체와는 무관(다른 파일명 조합 다수 확인), 순수 경로 문자열 인식 쪽의 우연한
  버그로 추정되지만, 재현 가능하고 회피 불가능함. 짧은 경로로 파싱을 통과시켜도 결국
  "세션에 공유되지 않은 파일" 권한 오류로 막히는 건 동일 — 이 메모의 기존 결론(근본 원인은
  경로 파싱이 아니라 세션 자체가 로컬 경로 업로드 권한이 없는 것)이 다시 확인됨.
- 결론 변동 없음: 사람이 직접 GitHub 업로드 페이지에서 드래그앤드롭 + Commit 하는 것 외에
  자동화 경로 없음.
